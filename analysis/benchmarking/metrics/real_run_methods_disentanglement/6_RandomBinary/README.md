

To run 'randnom binary' baseline on the reall dataasets:
- You can use the environment in which MintFlow is installed.

- In `./run.sh`, modify `conda activate` so your actual conda environment is activated.
- Run `./run.sh`.
- The results will be `./NonGit/Runs_TisssuesCombined/`.
- Afte the script finishes, in each subfolder there will be a dataframe `df_metrics_Fig1h.csv`.

